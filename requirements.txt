# Install torch, torchvision, and flash-attn separately for your CUDA stack first.
# Example (adjust to your machine/CUDA):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
#   pip install flash-attn --no-build-isolation

accelerate==1.7.0
deepspeed==0.17.1
numpy==2.2.6
peft==0.17.1
Pillow==12.2.0
pycocotools==2.0.11
safetensors==0.7.0
scipy==1.17.1
sentencepiece==0.2.1
tqdm==4.67.3
transformers==4.57.6
